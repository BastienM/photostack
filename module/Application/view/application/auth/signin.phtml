<header>
  <nav class="navbar navbar-default" role="navigation">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
          <a class="navbar-brand" href="<?php echo $this->url('home'); ?>"><i class="fa fa-stack-overflow"></i> Photostack</a>
      </div>
      <div class="navbar-collapse collapse" id="loginForm">
        <ul class="nav navbar-nav">
          <li><a href="<?php echo $this->url('home'); ?>">Home</a></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Galleries <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <?php foreach ($this->usersList as $user)
              {
              ?>
              <li>
                 <a href="<?php echo $this->url('gallery', array('user' => $user['username'])); ?>" title="<?php echo $user['username']; ?>">
                    <i class='fa fa-folder-open'></i> <?php echo $user['username']; ?>
                  </a>
              </li>
              <?php
              }
               ?>
            </ul>
          </li>
        </ul>
        <div class="navbar-form navbar-right">
          <a class="btn btn-info btn-sm" href="<?php echo $this->url('signup'); ?>"> Register</a>
        </div>
      </div>
    </div><!--/.navbar-collapse -->
  </nav>
</header><!-- /header -->
<section class="container">
  <div id="form" class="form-signin">
    <?php
    $form->setAttribute('action', $this->url('signin'))
    ->prepare();
    echo $this->form()->openTag($form);
    $users = $form->get('users');
    ?>
    <h2 class="form-signin-heading">Please sign in</h2>
    <hr>
    <?php
    echo "<div class='form-group'>";
    echo $this->formRow($users->get('mail'));
    echo "</div>";
    echo "<div class='form-group'>";
    echo $this->formRow($users->get('password'));
    echo "</div>";
    if (isset($this->error)) {
      echo "<div class='alert alert-danger alert-dismissable'>
              <button type='button' class='close' data-dismiss='alert' aria-hidden='true'>&times;</button>
              ".$this->error."
            </div>";
    }
    echo "<div class='form-group'>";
    echo $this->formElement($form->get('submit'));
    echo "<div class='form-group'>";
    echo $this->formHidden($form->get('csrf'));
    echo $this->form()->closeTag(); ?>
    <a class="btn btn-link btn-sm" id="btn-return" href="<?php echo $this->url('home'); ?>"><i class="fa fa-angle-double-right"></i> return to home</a>
  </div>
</section>